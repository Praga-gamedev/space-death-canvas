import React from 'react';

import { Button } from '@components/Button';

// import gif404 from '@images/404.svg';

export const NotFoundPage = () => {
    return <Button children={'Вход'} onClick={() => console.log('Вход')} />;
};
