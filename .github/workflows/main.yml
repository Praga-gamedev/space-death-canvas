name: CI

on:
  push:
    branches: [ features/docker ]
  pull_request:
    branches: [ features/docker ]

jobs:

  build:

    runs-on: self-hosted
    env:
      DB_PORT: 5432

    steps:
    - uses: actions/checkout@v1
    - name: build
      run: docker-compose up -d
