name: CI

on:
  push:
    branches: [ features/docker ]
  pull_request:
    branches: [ features/docker ]

jobs:

  build:

    runs-on: self-hosted

    steps:
    - uses: actions/checkout@v1
    - name: build
      run: echo "DB_PORT $DB_PORT" 
      env:
        DB_PORT: 5432
